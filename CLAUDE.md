Plain HTML/CSS only for now; ask before adding JavaScript, a framework, or a dependency.
Keep index.html at the repo root.
Prefer simple, readable code over clever solutions, and explain what you changed and why — I'm new to web dev.
Make reasonable design/style calls yourself rather than asking for every small decision, but tell me what you chose.
Write accessible, semantic HTML by default (alt text, proper heading order); don't edit DECISIONS.md.
