.DS_Store
Thumbs.db
node_modules
dist

# Secrets: never commit these. See .env.example
.env
.env.*
!.env.example

# Full-size original photos (kept locally, not uploaded)
originals/

# Local design-tool notes
.impeccable/
