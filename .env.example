# Copy this file to `.env` (same folder) and fill in your values.
# `.env` is listed in .gitignore, so it is never uploaded to GitHub.
# Never put these values in src/ or in any file that is committed.
# Get them at https://developer.spotify.com/dashboard (create an app).
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
